{{- if .Values.chat.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "neuroface.fullname" . }}-chat
  labels:
    {{- include "neuroface.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook-weight: "-10"
{{- end }}
